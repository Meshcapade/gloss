//TODO check that the vis optiosn are valid.
//For example check for every renderable mesh that has VisMesh that the colortype PerVert can be actually set (check if we have a color component on the entity). If we don't set the colort type back to Solid and emit an error
