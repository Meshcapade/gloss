[package]
name = "gloss_view_points"
version = "0.1.0"
edition = "2021"
rust-version = "1.69"
publish = false
doc = false

[dependencies]
gloss-renderer = { path = "../../crates/gloss_renderer", default-features = false }

# Third party
pollster.workspace=true
nalgebra.workspace=true

[lints]
workspace = true

[package.metadata.docs.rs]
targets = []  # Do not generate documentation for example